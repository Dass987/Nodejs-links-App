<div class="container p-4">
	<div class="row">
		<div class="col-md-8 mx-auto p-4">
			<div class="card text-center">
				<div class="card-body">
					<h3>Welcome {{user.fullname}}</h3>
					<h4>{{user.username}}</h4>
					<a href="/links" class="btn btn-primary mt-3">Save your links</a>
				</div>
			</div>
		</div>
	</div>
</div>